{##
 # Buy with wallet utility widget to monitor product price changes after option is selected
 #}
<div class="{{ this.fingerprint }}">
  <script>
    if ('undefined' !== typeof core) {
      core.trigger('xpaymentsBuyWithWalletPriceUpdate', { price: {{ this.product.getDisplayPrice() }} });
    }
  </script>
</div>